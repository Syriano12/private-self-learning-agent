# Test Report — 2026-09-03

## النطاق

اختُبرت نواة الوكيل في مهمة Web Research عامة باللغة العربية، دون Workflow مخصص للمثال. الهدف كان العثور على خمسة مصادر لمقارنة مشاريع مفتوحة المصدر لبناء وكيل محلي وقابل للتشغيل على Termux.

## النتائج

| الاختبار | النتيجة | الدليل |
|---|---|---|
| بناء ملفات Python | ناجح | `compileall` اكتمل دون أخطاء |
| `SELF_LEARNING_BEHAVIOR_TEST` | ناجح | `1 passed` |
| البحث الواقعي الأول | ناجح بعد الإصلاح | عُثر على 5 مصادر، وجُلبت 5 صفحات، والتحقق `True` |
| المهمة اللاحقة | ناجحة | استُرجعت 5 عناصر معرفة سابقة، والتحقق `True` |
| إعادة المحاولة | عملت | المهمة اللاحقة احتاجت محاولتين بسبب عدم كفاية النتيجة الأولى |

## الفشل المكتشف والإصلاح

فشل التشغيل الواقعي الأول في مرحلة الجلب لأن محرك البحث أعاد روابط DuckDuckGo بصيغة نسبية تبدأ بـ `//` وتحتوي على رابط الهدف داخل معامل `uddg`. كانت النتيجة الصحيحة هي رفض الادعاء بالنجاح: وُجدت خمسة نتائج، لكن لم تُجلب أدلة قابلة للتحقق. أُصلحت أداة البحث بإضافة تطبيع البروتوكول وفك رابط إعادة التوجيه، ثم أُعيد الاختبار ونجح.

هذا السلوك مهم لأنه يثبت أن التحقق ليس مجرد عداد نتائج البحث، وأن الفشل يُكتشف ويؤثر في محاولة التنفيذ التالية.

## حدود ما يثبت حتى الآن

يثبت الإصدار الحالي الاسترجاع الدائم للمعرفة، وإدخالها في rationale الخطة، والبحث متعدد المصادر، وإعادة المحاولة، والتحقق الأساسي من الأدلة. لكنه لا يثبت بعد استخدام نموذج لغوي لتوليد خطة غنية أو استخراج حقول الترخيص واللغة والمتطلبات من كل مشروع. كما أن البحث الدلالي الحالي بالكلمات المفتاحية، وسيُستبدل لاحقاً بمسترجع هجين عند إضافة طبقة embeddings اختيارية.

## قرار الإصدار

الإصدار `0.1.0` صالح كنواة تجريبية قابلة للنقل إلى Termux، وليس بعدُ نظام إنتاج نهائياً. لا ينبغي تفعيل WhatsApp أو تنفيذ إجراءات خارجية حساسة قبل توسيع Approval Gate وإضافة اختبارات أمنية وتحقق مستقل أكثر صرامة.
